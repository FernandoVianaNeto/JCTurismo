import P from 'prop-types';
import * as Styled from './styles';

export const PackagePageCard = () => {
  return (
    <Styled.Container>

    </Styled.Container>
  );
};

PackagePageCard.propTypes = {

}
