import P from 'prop-types';
import * as Styled from './styles';

export const Pacote = ({  }) => {
  return (
    <Styled.Container>

    </Styled.Container>
  );
};

Pacote.propTypes = {
  children: P.node.isRequired,
}
