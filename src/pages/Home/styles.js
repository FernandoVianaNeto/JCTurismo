import styled, { css } from 'styled-components';

export const Container = styled.div`
  ${({theme}) => css`


    .image-background {
      z-index: -1;
      position: absolute;
      width: 100%;
      height: 100%;
      opacity: 0.8;
    }

    .content {
      z-index: 1;
      opacity: 1;

      .icon {
        color: #FFF;
      }
    }

    .
  `}
`;
